# Copy to apps/dashboard/.env.local for local development.
# Base URL of the Pulseed API (apps/api). Server-side only: the browser reaches
# it through the dashboard's /api/* proxy.
API_URL=http://localhost:4000
