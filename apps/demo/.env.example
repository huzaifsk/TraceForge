# Copy to apps/demo/.env.local. Run `pnpm db:seed` (or create a project in the
# dashboard) and paste its DSN here.
NEXT_PUBLIC_TRACEFORGE_DSN=http://pk_xxx@localhost:4000/project/tf_xxxxxxxxxxxx
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000
